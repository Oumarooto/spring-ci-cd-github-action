spring:
  datasource:
    url: jdbc:mysql://localhost:3306/product_db
    username: user
    password: password
  jpa:
    hibernate:
      ddl-auto: update # Met à jour la table automatiquement en dev
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.maryam: DEBUG # Plus de détails en développement